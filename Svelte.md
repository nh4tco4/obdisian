UI framework for building web applications
Svelte web application is based on components that encapsulate TypeScript (js as well), CSS and HTML

Everything is stored inside `.svelte` files

---
[[CSS]] [[HTML]] [[TypeScript]]